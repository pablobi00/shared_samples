# environment.conf
# Tells Puppet where to find modules in this environment.
# site-modules/ holds our local role and profile modules.
# modules/ holds our local component modules (module_1, module_2, module_3).
# $basemodulepath appends the global module path configured on the Puppet server.
modulepath = site-modules:modules:$basemodulepath
